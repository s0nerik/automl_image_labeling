name: automl_image_labeling_example
description: Demonstrates how to use the automl_image_labeling plugin.

publish_to: 'none'

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  automl_image_labeling:
    path: ../
  camera2:
    git:
      url: https://github.com/s0nerik/camera2.git
      ref: 783036503bb92d229be7c1d9dbb01843a3cdab26
  permission_handler: ^5.0.1+1
  image: ^2.1.19

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/test_model/